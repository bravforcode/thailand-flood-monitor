import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'thailand-flood-monitor',
  webDir: 'build'
};

export default config;
